{
  "extends": "solhint:recommended",
  "rules": {
    "max-states-count": "off",
    "compiler-version": ["error", "^0.7.6"],
    "reason-string": [
      "warn",
      {
        "maxLength": 64
      }
    ]
  }
}
