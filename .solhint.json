{
  "extends": "solhint:recommended",
  "rules": {
    "max-states-count": "off",
    "compiler-version": ["error", "^0.8.9"],
    "reason-string": [
      "warn",
      {
        "maxLength": 64
      }
    ]
  }
}
